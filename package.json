{
    "name": "@coveops/dual-field-facet-slider",
    "version": "1.0.2",
    "description": "Slider that works with 2 fields from the coveo index",
    "main": "dist/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "coveops build DualFieldFacetSlider --template typescript",
        "watch": "coveops build DualFieldFacetSlider --template typescript --watch",
        "serve": "coveops serve",
        "precompile": "npm run build"
    },
    "keywords": [],
    "author": "",
    "license": "Apache-2.0",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "dependencies": {
        "@coveops/localization-manager": "^1.0.0",
        "@coveops/turbo-core": "^1.1.1",
        "coveo-search-ui": "^2.9856.8"
    },
    "devDependencies": {
        "@coveops/cli": "^0.13.2"
    }
}
